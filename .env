# Example env file for docker-compose
# Copy to docker-setup/.env and edit before running

# Postgres credentials (do NOT commit real passwords)
POSTGRES_DB=monster
POSTGRES_USER=system
POSTGRES_PASSWORD=manager

# MonsterMQ image to use (set to your registry/image if you have one)
MONSTERMQ_IMAGE=rocworks/monstermq:latest
